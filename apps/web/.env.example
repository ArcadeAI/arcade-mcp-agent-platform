# ============================================================================
# Open Agent Platform + Arcade MCP - Environment Configuration
# ============================================================================

# ----------------------------------------------------------------------------
# Base Configuration
# ----------------------------------------------------------------------------

NEXT_PUBLIC_BASE_API_URL=http://localhost:3000/api
NODE_ENV=development
NEXT_PUBLIC_DEMO_APP=false

# ----------------------------------------------------------------------------
# NextAuth + SSO Configuration
# ----------------------------------------------------------------------------

# NextAuth Secret - Generate with: openssl rand -base64 32
NEXTAUTH_SECRET=your-nextauth-secret-here
NEXTAUTH_URL=http://localhost:3000

# Identity Providers - Configure at least ONE of the following:
# The system automatically detects which providers are configured and makes them available

# === Option 1: Okta ===
OKTA_CLIENT_ID=your-okta-client-id
OKTA_CLIENT_SECRET=your-okta-client-secret
OKTA_ISSUER=https://your-domain.okta.com/oauth2/default

# === Option 2: Microsoft Entra ID (Azure AD) ===
ENTRA_CLIENT_ID=your-azure-client-id
ENTRA_CLIENT_SECRET=your-azure-client-secret
ENTRA_TENANT_ID=your-azure-tenant-id

# === Option 3: PingID ===
PING_CLIENT_ID=your-ping-client-id
PING_CLIENT_SECRET=your-ping-client-secret
PING_ISSUER=https://auth.pingone.com/{environment-id}/as

# ----------------------------------------------------------------------------
# LangGraph Deployment Configuration
# ----------------------------------------------------------------------------

# CRITICAL: "id" must be the ASSISTANT ID from your LangGraph server
NEXT_PUBLIC_DEPLOYMENTS='[{"id":"<ASSISTANT_ID>","deploymentUrl":"http://localhost:2024","isDefault":true,"defaultGraphId":"chat_agent","name":"Local"}]'

NEXT_PUBLIC_USE_LANGSMITH_AUTH=true
LANGSMITH_API_KEY=lsv2_pt_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# ----------------------------------------------------------------------------
# Arcade MCP Gateway Configuration  
# ----------------------------------------------------------------------------

# Arcade MCP Gateway URL (full URL including /mcp/<slug>)
ARCADE_MCP_GATEWAY_URL=https://api.arcade.dev/mcp/<your-slug>

# Arcade Project API Key (server-side only)
ARCADE_API_KEY=arc_proj_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# Arcade User ID - OPTIONAL fallback for development
# Only needed if no SSO provider is configured
# In production with SSO, user email is automatically used from the session
ARCADE_USER_ID=your-email@example.com

NEXT_PUBLIC_MCP_AUTH_REQUIRED=true

# ============================================================================
# Multi-User vs Single-User Mode
# ============================================================================
#
# MULTI-USER (Production with SSO):
#   - Configure at least one identity provider above
#   - Each user signs in with their own credentials
#   - Arcade-User-ID is set to the authenticated user's email
#   - Each user has their own tool OAuth authorizations in Arcade
#   - ARCADE_USER_ID env var is ignored when users are authenticated
#
# SINGLE-USER (Development without SSO):
#   - Don't configure any identity providers
#   - Set ARCADE_USER_ID to a single email
#   - All users share the same Arcade identity
#   - Useful for local testing and development
#
